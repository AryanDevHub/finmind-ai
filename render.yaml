services:
  - type: web
    name: finmind-ai-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && python setup_db.py
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: SECRET_KEY
        value: finmind_secret_2025